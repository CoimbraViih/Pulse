# M0 — Project Scaffold Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Bootstrap a working Next.js 15 app with TypeScript strict, Tailwind CSS v4, shadcn/ui, Geist font, ESLint + Prettier, and a Vercel-deployed placeholder that shows a dark background with an orange accent.

**Architecture:** Single Next.js 15 App Router project scaffolded directly into the current working directory (`c:\Users\victo\OneDrive\Área de Trabalho\Pulse`). Design tokens are defined as CSS custom properties in `globals.css` using Tailwind v4's `@theme` directive. shadcn/ui is initialized on top of that, using the `dark` class strategy.

**Tech Stack:** Next.js 15 (App Router), TypeScript 5 strict, Tailwind CSS v4, shadcn/ui (canary for Tailwind v4 support), Geist font (next/font/google), ESLint (flat config), Prettier, Vercel CLI.

**Working directory:** `c:\Users\victo\OneDrive\Área de Trabalho\Pulse` — all commands run from here unless stated otherwise.

---

## File Map

| File | Action | Responsibility |
|------|--------|----------------|
| `package.json` | Created by scaffolder | Project metadata + deps |
| `next.config.ts` | Created by scaffolder, minor edit | Strict Next.js config |
| `tsconfig.json` | Created by scaffolder | TypeScript strict mode |
| `app/globals.css` | Edit | Tailwind v4 import + `@theme` design tokens |
| `app/layout.tsx` | Edit | Root layout, Geist font, dark class on `<html>` |
| `app/page.tsx` | Edit | Placeholder homepage |
| `components/ui/` | Generated by shadcn | shadcn/ui primitives |
| `.env.local.example` | Create | All future env var stubs |
| `eslint.config.mjs` | Edit | ESLint flat config with Next.js rules |
| `.prettierrc` | Create | Prettier formatting rules |
| `.prettierignore` | Create | Files Prettier should skip |

---

## Task 1: Scaffold Next.js 15 App

**Files:**
- Create: all root project files (via CLI)

- [ ] **Step 1.1: Run create-next-app in the current directory**

Open a terminal in `c:\Users\victo\OneDrive\Área de Trabalho\Pulse` and run:

```bash
npx create-next-app@latest . \
  --typescript \
  --tailwind \
  --eslint \
  --app \
  --src-dir=false \
  --import-alias="@/*" \
  --turbopack
```

When prompted:
- Would you like to use Tailwind CSS? → **Yes**
- Would you like your code inside a `src/` directory? → **No**
- What import alias would you like configured? → `@/*`

> **Note:** This will ask to overwrite files. Since only `CLAUDE.md`, `PLAN.md`, and `docs/` exist, answer **Yes** to any overwrite prompt for `package.json` / `next.config.ts` / etc.

- [ ] **Step 1.2: Verify the scaffold ran correctly**

```bash
ls
```

Expected output includes: `app/`, `components/`, `public/`, `package.json`, `next.config.ts`, `tsconfig.json`, `tailwind.config.ts` (or no separate tailwind config for v4).

- [ ] **Step 1.3: Verify dev server starts**

```bash
npm run dev
```

Open `http://localhost:3000`. Expected: default Next.js welcome page renders without errors. Stop the dev server (`Ctrl+C`).

- [ ] **Step 1.4: Commit**

```bash
git init
git add .
git commit -m "feat: scaffold Next.js 15 app with TypeScript, Tailwind, App Router"
```

---

## Task 2: Configure Tailwind CSS v4 Design Tokens

**Files:**
- Edit: `app/globals.css`
- Edit: `app/layout.tsx` (add `dark` class to `<html>`)

> **Tailwind v4 note:** There is no `tailwind.config.ts` in v4. All customization lives in `globals.css` via the `@theme` directive. If the scaffolder generated a `tailwind.config.ts`, delete it after this task.

- [ ] **Step 2.1: Replace `app/globals.css` with design tokens**

Replace the entire contents of `app/globals.css` with:

```css
@import "tailwindcss";

@theme {
  /* Brand */
  --color-accent: #f97316;        /* orange-500 */
  --color-accent-hover: #ea6c0a;  /* orange-600 */

  /* Backgrounds */
  --color-bg-base: #0a0a0a;       /* near-black */
  --color-bg-surface: #141414;    /* card background */
  --color-bg-elevated: #1f1f1f;   /* elevated surface */

  /* Borders */
  --color-border: #2a2a2a;
  --color-border-subtle: #1f1f1f;

  /* Text */
  --color-text-primary: #fafafa;
  --color-text-secondary: #a1a1aa; /* zinc-400 */
  --color-text-muted: #71717a;     /* zinc-500 */

  /* Energy levels */
  --color-energy-high: #f97316;   /* orange — high energy */
  --color-energy-medium: #eab308; /* yellow — medium energy */
  --color-energy-low: #3b82f6;    /* blue — low energy */

  /* Focus score */
  --color-score-good: #22c55e;    /* green */
  --color-score-warn: #eab308;    /* yellow */
  --color-score-bad: #ef4444;     /* red */

  /* Typography */
  --font-sans: "Geist", ui-sans-serif, system-ui, sans-serif;
}

/* Base resets */
* {
  border-color: var(--color-border);
}

body {
  background-color: var(--color-bg-base);
  color: var(--color-text-primary);
  font-family: var(--font-sans);
  -webkit-font-smoothing: antialiased;
}
```

- [ ] **Step 2.2: Delete `tailwind.config.ts` if it exists**

```bash
# Only run if the file exists
rm tailwind.config.ts
```

- [ ] **Step 2.3: Verify dev server still starts with no CSS errors**

```bash
npm run dev
```

No errors in terminal. Stop dev server.

- [ ] **Step 2.4: Commit**

```bash
git add app/globals.css
git rm --cached tailwind.config.ts 2>/dev/null || true
git commit -m "feat: configure Tailwind v4 design tokens with dark palette and orange accent"
```

---

## Task 3: Configure Geist Font and Root Layout

**Files:**
- Edit: `app/layout.tsx`

- [ ] **Step 3.1: Replace `app/layout.tsx` with Geist font setup**

```tsx
import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Pulse — Planejador de Alta Performance",
  description:
    "Sistema de decisão para empreendedores solos que querem planejar com inteligência.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="pt-BR" className="dark" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
```

> The `dark` class on `<html>` activates dark mode globally (shadcn/ui uses the `class` strategy). `suppressHydrationWarning` prevents React hydration warnings when a theme toggle eventually modifies the class.

- [ ] **Step 3.2: Verify layout renders correctly**

```bash
npm run dev
```

Open `http://localhost:3000`. Background should be near-black. No console errors. Stop dev server.

- [ ] **Step 3.3: Commit**

```bash
git add app/layout.tsx
git commit -m "feat: configure Geist font and dark-mode root layout"
```

---

## Task 4: Build the Placeholder Homepage

**Files:**
- Edit: `app/page.tsx`

- [ ] **Step 4.1: Replace `app/page.tsx` with branded placeholder**

```tsx
export default function HomePage() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center gap-6 px-4">
      <div className="flex flex-col items-center gap-3 text-center">
        <span className="text-5xl font-bold tracking-tight text-[var(--color-accent)]">
          Pulse
        </span>
        <p className="max-w-sm text-[var(--color-text-secondary)]">
          Seu sistema de decisão diário. Em breve.
        </p>
      </div>

      <div className="flex gap-2">
        <span className="h-2 w-2 rounded-full bg-[var(--color-accent)]" />
        <span className="h-2 w-2 rounded-full bg-[var(--color-bg-elevated)]" />
        <span className="h-2 w-2 rounded-full bg-[var(--color-bg-elevated)]" />
      </div>
    </main>
  );
}
```

- [ ] **Step 4.2: Verify the placeholder renders correctly**

```bash
npm run dev
```

Open `http://localhost:3000`. Verify:
- Background is near-black (`#0a0a0a`)
- "Pulse" text is orange (`#f97316`)
- Subtitle text is visible in zinc-400 color
- Orange dot + two dark dots visible

Stop dev server.

- [ ] **Step 4.3: Commit**

```bash
git add app/page.tsx
git commit -m "feat: add branded placeholder homepage"
```

---

## Task 5: Initialize shadcn/ui

**Files:**
- Edit: `components/utils.ts` or `lib/utils.ts` (created by shadcn init)
- Create: `components/ui/` (auto-generated — do not hand-edit)

> shadcn/ui currently requires Tailwind CSS v4 canary support. Use the `canary` tag.

- [ ] **Step 5.1: Run shadcn/ui init**

```bash
npx shadcn@canary init
```

When prompted:
- Which style would you like to use? → **Default**
- Which color would you like to use as the base color? → **Zinc**
- Would you like to use CSS variables for theming? → **Yes**

> shadcn init will modify `globals.css` and create `lib/utils.ts` (or `components/lib/utils.ts` depending on version). Accept all changes.

- [ ] **Step 5.2: Verify the `lib/utils.ts` file was created**

```bash
cat lib/utils.ts
```

Expected output (approximately):

```ts
import { type ClassValue, clsx } from "clsx";
import { twMerge } from "tailwind-merge";

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}
```

- [ ] **Step 5.3: Re-apply the custom design tokens to `globals.css`**

> shadcn init rewrites `globals.css`. Open `app/globals.css` and verify the `@theme` block from Task 2 is still present. If shadcn replaced it, add the `@theme` block back at the top (after `@import "tailwindcss"`). The final file should have both shadcn's CSS variable block and our `@theme` block.

The file should look like (merge both sections):

```css
@import "tailwindcss";

@theme {
  /* Brand */
  --color-accent: #f97316;
  --color-accent-hover: #ea6c0a;

  /* Backgrounds */
  --color-bg-base: #0a0a0a;
  --color-bg-surface: #141414;
  --color-bg-elevated: #1f1f1f;

  /* Borders */
  --color-border: #2a2a2a;
  --color-border-subtle: #1f1f1f;

  /* Text */
  --color-text-primary: #fafafa;
  --color-text-secondary: #a1a1aa;
  --color-text-muted: #71717a;

  /* Energy levels */
  --color-energy-high: #f97316;
  --color-energy-medium: #eab308;
  --color-energy-low: #3b82f6;

  /* Focus score */
  --color-score-good: #22c55e;
  --color-score-warn: #eab308;
  --color-score-bad: #ef4444;

  /* Typography */
  --font-sans: "Geist", ui-sans-serif, system-ui, sans-serif;
}

/* shadcn/ui CSS variables — do not remove */
:root {
  --background: 0 0% 4%;
  --foreground: 0 0% 98%;
  /* ... rest of shadcn vars ... */
}

.dark {
  --background: 0 0% 4%;
  --foreground: 0 0% 98%;
  /* ... rest of shadcn dark vars ... */
}

* {
  border-color: var(--color-border);
}

body {
  background-color: var(--color-bg-base);
  color: var(--color-text-primary);
  font-family: var(--font-sans);
  -webkit-font-smoothing: antialiased;
}
```

- [ ] **Step 5.4: Install a test shadcn component to verify setup**

```bash
npx shadcn@canary add button
```

Expected: `components/ui/button.tsx` is created.

- [ ] **Step 5.5: Add the Button to the placeholder to verify shadcn works**

Edit `app/page.tsx` — add the import and a button:

```tsx
import { Button } from "@/components/ui/button";

export default function HomePage() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center gap-6 px-4">
      <div className="flex flex-col items-center gap-3 text-center">
        <span className="text-5xl font-bold tracking-tight text-[var(--color-accent)]">
          Pulse
        </span>
        <p className="max-w-sm text-[var(--color-text-secondary)]">
          Seu sistema de decisão diário. Em breve.
        </p>
      </div>

      <Button className="bg-[var(--color-accent)] text-white hover:bg-[var(--color-accent-hover)]">
        Começar
      </Button>

      <div className="flex gap-2">
        <span className="h-2 w-2 rounded-full bg-[var(--color-accent)]" />
        <span className="h-2 w-2 rounded-full bg-[var(--color-bg-elevated)]" />
        <span className="h-2 w-2 rounded-full bg-[var(--color-bg-elevated)]" />
      </div>
    </main>
  );
}
```

- [ ] **Step 5.6: Verify dev server + shadcn button renders**

```bash
npm run dev
```

Open `http://localhost:3000`. Orange "Começar" button should be visible with correct styling. No TypeScript or CSS errors. Stop dev server.

- [ ] **Step 5.7: Commit**

```bash
git add .
git commit -m "feat: initialize shadcn/ui with dark mode and Button component verified"
```

---

## Task 6: Configure ESLint and Prettier

**Files:**
- Edit: `eslint.config.mjs`
- Create: `.prettierrc`
- Create: `.prettierignore`

- [ ] **Step 6.1: Install Prettier and the ESLint-Prettier integration**

```bash
npm install --save-dev prettier eslint-config-prettier prettier-plugin-tailwindcss
```

- [ ] **Step 6.2: Create `.prettierrc`**

```json
{
  "semi": true,
  "singleQuote": false,
  "tabWidth": 2,
  "trailingComma": "es5",
  "printWidth": 100,
  "plugins": ["prettier-plugin-tailwindcss"]
}
```

- [ ] **Step 6.3: Create `.prettierignore`**

```
.next/
node_modules/
public/
*.md
```

- [ ] **Step 6.4: Update `eslint.config.mjs` to add Prettier**

Open the existing `eslint.config.mjs` (created by create-next-app). Add `prettier` to the extends:

```js
import { FlatCompat } from "@eslint/eslintrc";
import path from "path";
import { fileURLToPath } from "url";

const __filename = fileURLToPath(import.meta.url);
const __dirname = path.dirname(__filename);

const compat = new FlatCompat({
  baseDirectory: __dirname,
});

const eslintConfig = [
  ...compat.extends("next/core-web-vitals", "next/typescript", "prettier"),
];

export default eslintConfig;
```

- [ ] **Step 6.5: Run linting and confirm it passes**

```bash
npm run lint
```

Expected: no errors.

- [ ] **Step 6.6: Run Prettier formatting**

```bash
npx prettier --write .
```

Expected: files reformatted or "unchanged". No errors.

- [ ] **Step 6.7: Add lint and format scripts to `package.json`**

Open `package.json`. In the `"scripts"` block, add:

```json
"scripts": {
  "dev": "next dev --turbopack",
  "build": "next build",
  "start": "next start",
  "lint": "next lint",
  "format": "prettier --write .",
  "format:check": "prettier --check ."
}
```

- [ ] **Step 6.8: Commit**

```bash
git add .
git commit -m "feat: add Prettier with Tailwind plugin and ESLint-Prettier integration"
```

---

## Task 7: Create `.env.local.example`

**Files:**
- Create: `.env.local.example`

- [ ] **Step 7.1: Create `.env.local.example` with all future variable stubs**

```bash
# ============================================================
# Pulse — Environment Variables
# Copy this file to .env.local and fill in real values.
# NEVER commit .env.local to git.
# ============================================================

# Supabase (M11)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Stripe (M13)
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
NEXT_PUBLIC_STRIPE_PRICE_ID=

# Email — Resend (M14)
RESEND_API_KEY=

# App
NEXT_PUBLIC_APP_URL=http://localhost:3000
```

- [ ] **Step 7.2: Verify `.env.local` is in `.gitignore`**

```bash
cat .gitignore | grep env
```

Expected: `.env*.local` appears in the output (create-next-app adds this by default).

- [ ] **Step 7.3: Commit**

```bash
git add .env.local.example
git commit -m "feat: add .env.local.example with all future variable stubs"
```

---

## Task 8: TypeScript Strict Mode Verification

**Files:**
- Edit: `tsconfig.json` (verify strict flags are set)

- [ ] **Step 8.1: Verify `tsconfig.json` has strict mode enabled**

Open `tsconfig.json`. The `compilerOptions` must include:

```json
{
  "compilerOptions": {
    "strict": true,
    "noUncheckedIndexedAccess": true,
    "noImplicitOverride": true
  }
}
```

If `noUncheckedIndexedAccess` and `noImplicitOverride` are missing, add them. `strict: true` already enables: `strictNullChecks`, `strictFunctionTypes`, `noImplicitAny`, etc.

- [ ] **Step 8.2: Run the TypeScript compiler to confirm no errors**

```bash
npx tsc --noEmit
```

Expected: no output (exit 0). If errors appear, fix them before proceeding.

- [ ] **Step 8.3: Commit if tsconfig was changed**

```bash
git add tsconfig.json
git commit -m "chore: ensure TypeScript strict + noUncheckedIndexedAccess enabled"
```

---

## Task 9: Vercel Deployment

**Files:**
- Create: `.vercelignore` (optional)

- [ ] **Step 9.1: Install Vercel CLI globally**

```bash
npm install -g vercel
```

- [ ] **Step 9.2: Log in to Vercel**

```bash
vercel login
```

Authenticate via browser with the account `viihcoimbra7x@gmail.com`. Choose the email option when asked.

- [ ] **Step 9.3: Link the project to Vercel**

```bash
vercel link
```

When prompted:
- Set up and deploy: **Yes**
- Which scope? → Choose your personal account
- Link to existing project? → **No**
- Project name: → `pulse` (or `pulse-app` if `pulse` is taken)
- In which directory is your code located? → `.` (current directory)

This creates a `.vercel/` folder with `project.json`. Do not commit `.vercel/` (it's in `.gitignore` by default).

- [ ] **Step 9.4: Run a production build locally to verify no build errors**

```bash
npm run build
```

Expected output ends with:
```
✓ Compiled successfully
Route (app) ...
```

No TypeScript errors. No ESLint errors (Next.js build runs lint by default).

- [ ] **Step 9.5: Deploy to Vercel**

```bash
vercel --prod
```

Expected: deployment URL printed, e.g. `https://pulse-xxx.vercel.app`.

- [ ] **Step 9.6: Open the live URL and verify**

Open the URL from Step 9.5 in a browser. Verify:
- Dark background renders
- "Pulse" in orange renders
- "Começar" button visible
- No broken layout or 500 errors

- [ ] **Step 9.7: Final commit with deployment info**

```bash
git add .
git commit -m "chore: link Vercel project — M0 scaffold complete"
```

---

## Self-Review Checklist

### Spec Coverage

| M0 Deliverable | Task |
|----------------|------|
| `create-next-app` with TypeScript strict, App Router, Tailwind CSS v4 | Task 1, Task 8 |
| shadcn/ui initialized (dark mode, orange-500 accent in CSS vars) | Task 2, Task 5 |
| Geist font configured in `app/layout.tsx` | Task 3 |
| ESLint + Prettier configured and passing | Task 6 |
| `.env.local.example` with all future variable names stubbed | Task 7 |
| Vercel project linked, first deploy green | Task 9 |
| `/` renders placeholder with dark background and orange accent | Task 4 + Task 5 |

All deliverables covered. ✓

### Done-When Criteria

| Criterion | Task |
|-----------|------|
| `/` renders placeholder with dark background | Task 4 step 4.2 |
| Orange accent visible | Task 4 step 4.2 |
| `vercel deploy` passes | Task 9 step 9.5–9.6 |

---

## Notes for Executor

- **Windows paths:** All commands can be run in PowerShell or Git Bash. If using PowerShell, replace `rm` with `Remove-Item`.
- **Tailwind v4:** No `tailwind.config.ts` needed — all config is in CSS. If `create-next-app` generates one, delete it after Task 2.
- **shadcn canary:** Use `npx shadcn@canary` throughout — the stable release does not fully support Tailwind v4 yet.
- **Git:** The project has no git repo yet — Task 1 initializes it. All subsequent commits build on that.
- **Node version:** Use Node 20+ (required by Next.js 15).
